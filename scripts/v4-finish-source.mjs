// Temporary reconciliation of authored, hash-checked files. Removed before final delivery.
import fs from 'node:fs';import path from 'node:path';import{createHash}from'node:crypto';import{execFileSync}from'node:child_process';
const change=(p,a,b)=>{const s=fs.readFileSync(p,'utf8');if(s.split(a).length!==2)throw Error('Expected unique source anchor: '+p+' '+a.slice(0,120));fs.writeFileSync(p,s.replace(a,b));};
const prepend=(p,s)=>fs.writeFileSync(p,s+fs.readFileSync(p,'utf8'));
const files=['src/adapters/blender/validation.ts','src/adapters/blender/drivers.ts','src/adapters/blender/runner.ts','src/ui/tui/form.tsx','src/ui/tui/skill-wizard.ts','tests/fake-provider/pi-service.ts','tests/contract/skills-pi.test.ts','tests/e2e/skills-workflow.test.ts','scripts/prepare-blender.mjs','examples/blender-asset/scene.py','tests/e2e/blender.test.ts','tests/security/blender.test.ts'];
const tree=JSON.parse(execFileSync('gh',['api','repos/vellxw/Perfect-harness/git/trees/dfa2e6ac1d0574be65aff2583d77c0f7462ab1ac?recursive=1'],{encoding:'utf8'}));if(tree.truncated)throw Error('Truncated authored tree');
for(const p of files){if(fs.existsSync(p))throw Error('Do not overwrite newer source '+p);const e=tree.tree.find(e=>e.path===p);if(!e||e.mode!=='100644'||e.type!=='blob'||e.size>100000)throw Error('Invalid authored entry '+p);const blob=JSON.parse(execFileSync('gh',['api','repos/vellxw/Perfect-harness/git/blobs/'+e.sha],{encoding:'utf8'}));const bytes=Buffer.from(blob.content,'base64');if(createHash('sha1').update(Buffer.from(`blob ${bytes.length}\0`)).update(bytes).digest('hex')!==e.sha)throw Error('Authored blob mismatch');fs.mkdirSync(path.dirname(p),{recursive:true});fs.writeFileSync(p,bytes);}
const wizard='src/ui/tui/skill-wizard.ts';
for(const [before,after]of [['Consume la cuota del agente configurado.","CREAR"','Consume la cuota del agente configurado.`,"CREAR"'],['Entra en cuarentena.","RECOPILAR"','Entra en cuarentena.`,"RECOPILAR"'],['Después hay que inspeccionar, autorizar y ejecutar.","GUARDAR"','Después hay que inspeccionar, autorizar y ejecutar.`,"GUARDAR"']])change(wizard,before,after);
change('tests/e2e/skills-workflow.test.ts','if(step===1)return {result:{summary:"Fixture completado",outputs:[evaluation?"solution.mjs":"calcular-doble/SKILL.md"]}};','if(step===1)return {result:evaluation?{summary:"Fixture completado"}:{summary:"Fixture completado",outputs:["calcular-doble/SKILL.md"]}};');
change('src/adapters/blender/runner.ts','{join,dirname,resolve}','{join,dirname}');
// Three package exports are checked against the installed runtime, not assumed.
change('src/adapters/blender/runner.ts','const threeRoot=dirname(require.resolve("three/package.json"));','const threeRoot=dirname(dirname(require.resolve("three")));');
// Verification remains controller-owned; adapters cannot declare DONE.
change('src/domain/model.ts','kind: z.enum(["command", "browser", "remotion", "postgres"]),','kind: z.enum(["command", "browser", "remotion", "postgres", "blender"]),');
change('src/domain/model.ts','    remotion: z\n      .object({','    blender:z.object({script:z.string().min(1),sourceFile:z.string().default("source.blend"),width:z.number().int().min(128).max(1920).default(640),height:z.number().int().min(128).max(1080).default(480),timeoutMs:z.number().int().min(1000).max(1200000).default(240000),maxBytes:z.number().int().min(100000).max(32000000).default(16000000)}).strict().optional(),\n    remotion: z\n      .object({');
change('src/domain/model.ts','    if (s.kind === "browser" && !s.scenario)','    if(s.kind === "blender" && !s.blender)ctx.addIssue({code:"custom",message:"Blender specification required"});\n    if (s.kind === "browser" && !s.scenario)');
change('src/ports/execution.ts','export interface ExecutionRunner {','export interface ExecutionRunner {\n  blender?(request:ExecutionRequest,spec:{script:string;sourceFile:string;width:number;height:number;timeoutMs:number;maxBytes:number}):Promise<ExecutionOutput>;');
prepend('src/adapters/sandbox/prepared-runner.ts','import{BlenderRunner}from"../blender/runner.js";\n');
change('src/adapters/sandbox/prepared-runner.ts','  available(): Promise<boolean> {','  blender(request:ExecutionRequest,spec:{script:string;sourceFile:string;width:number;height:number;timeoutMs:number;maxBytes:number}):Promise<ExecutionOutput>{return this.admission.use(request.signal,()=>new BlenderRunner(this.config,this.store,join(this.directory,"blender")).run(request,spec));}\n  available(): Promise<boolean> {');
change('src/adapters/verification/service.ts','      if (spec.kind === "postgres") {','      if(spec.kind === "blender"){if(!this.runner.blender)throw new Blocked("BLENDER_RUNNER_REQUIRED","Falta el runner aislado de Blender");output=await this.runner.blender(request,spec.blender!);}\n      else if (spec.kind === "postgres") {');
// Guided forms preserve exact bindings and fit the same small-terminal layout.
fs.writeFileSync('src/ui/tui/binding-form.tsx',`import type{AgentProfile}from"../../skills/model.js";import{BindingSchema}from"../../skills/model.js";import type{Motion}from"../../presentation/protocol.js";import{GuidedForm}from"./form.js";
export function BindingForm({profile,width,height,motion,onSave,onCancel}:{profile:AgentProfile;width:number;height:number;motion:Motion;onSave:(binding:AgentProfile["binding"])=>void;onCancel:()=>void}){return <GuidedForm width={width} height={height} motion={motion} onCancel={onCancel} spec={{title:"Modelo · "+profile.name,fields:[{key:"provider",label:"Proveedor",initial:profile.binding.provider},{key:"model",label:"Modelo exacto",initial:profile.binding.model},{key:"reasoning",label:"Razonamiento",initial:profile.binding.reasoning,choices:["off","minimal","low","medium","high","xhigh","max"].map(value=>({value,label:value}))},{key:"accountRef",label:"Cuenta local",initial:profile.binding.accountRef},{key:"auth",label:"Autenticación",initial:profile.binding.auth,choices:[{value:"oauth",label:"OAuth"},{value:"api_key",label:"API key local"}]},{key:"billingMode",label:"Facturación",initial:profile.binding.billingMode,choices:[{value:"subscription",label:"Suscripción"},{value:"free",label:"Gratuita"},{value:"metered",label:"Por consumo (requiere permiso aparte)"}]}],submit:values=>onSave(BindingSchema.parse({...profile.binding,...values}))}}/>;}
`);
prepend('src/presentation/protocol.ts','import{DraftBriefSchema}from"../skills/creator.js";\n');
change('src/presentation/protocol.ts','export type Screen =','export type Screen =\n  | "skill-trials" | "trial-detail"');
change('src/presentation/protocol.ts','export const UiActionSchema = z.discriminatedUnion("type", [','export const UiActionSchema = z.discriminatedUnion("type", [\n z.object({type:z.literal("create-skill-brief"),brief:DraftBriefSchema,confirmation:z.literal("CREAR").default("CREAR")}).strict(),');
prepend('src/presentation/engine.ts','import{draftInstruction}from"../skills/creator.js";\n');
change('src/presentation/engine.ts','  readonly studios: StudioAdmin;','  readonly studios: StudioAdmin;\n  private studioTask?:Promise<void>;');
change('src/presentation/engine.ts','  private idle(): void {','  private idle(): void {\n    if(this.studioTask)throw new Blocked("EVAL_BUSY","Hay una evaluación de skills activa; cancelala o esperá antes de iniciar otro trabajo");');
change('src/presentation/engine.ts','        case "studio": {','        case "create-skill-brief": {\n          this.idle();\n          const cfg=await ctx.config(),studio=this.studios.panel(this.workspace,cfg);\n          if(!studio.config.sets.some(s=>s.id===action.brief.team&&s.enabled))throw new Blocked("SKILL_CREATOR_TEAM","Equipo no disponible");\n          const goal=await createGoal({request:draftInstruction(action.brief),source:this.workspace,home:this.home,config:cfg,privacy:"private",workMode:"skill-studio"},this.store);\n          this.start(goal);message="Creación privada iniciada. El resultado será un borrador; aprobación y activación son independientes.";break;\n        }\n        case "studio": {\n          if(action.action.command === "trial-run"){\n            this.idle();const configuration=await ctx.config();\n            this.studioTask=this.studios.perform(this.workspace,configuration,action.action,AbortSignal.timeout(1200000)).then(result=>{this.send({type:"result",requestId,ok:true,message:result.message,content:result.content,operation:"studio"});}).catch(error=>{this.send({type:"result",requestId,ok:false,message:error instanceof Error?error.message:"Evaluación interrumpida",operation:"studio"});}).finally(()=>{this.studioTask=undefined;this.publish();});\n            message="Evaluación iniciada. /evaluaciones permite ver progreso o cancelar sin bloquear la interfaz.";break;\n          }');
change('src/presentation/engine.ts','  async dispose(): Promise<void> {','  async dispose(): Promise<void> {\n    await this.studios.close();\n    await this.studioTask;');
// User-facing record summaries, not extra model prompt content.
change('src/skills/experiments.ts','    trialId: t.id,\n    status: t.status,','    trialId: t.id,\n    releaseId:t.spec.releaseId,baselineReleaseId:t.spec.baselineReleaseId,profileId:t.spec.profileId,partition:t.spec.partition,repetitions:t.spec.repetitions,budget:{maxRequests:t.spec.maxRequests,maxTokens:t.spec.maxTokens},\n    status: t.status,');
// Bound execution ownership to a transaction. No simultaneous personal-provider goals in this small evaluator.
change('src/skills/experiments.ts','  trial = {\n    ...trial,\n    status: "running",','  if(store.list("runs").some(r=>r.status === "running"))throw new Blocked("EVAL_PROVIDER_BUSY","Otra sesión está usando proveedores. Pausá ese trabajo antes de evaluar.");\n  trial = store.transaction(()=>{const current=store.get("skillTrials",input.trialId);if(!current||current.status!=="authorized"||current.attempts!==trial!.attempts)throw new Blocked("EVAL_BUSY","Otro controlador inició la evaluación");const claimed:TrialRecord = {\n    ...current,\n    status: "running",');
change('src/skills/experiments.ts','  };\n  store.put("skillTrials", trial, "skill.trial_started");','  };store.put("skillTrials",claimed,"skill.trial_started");return claimed;});');
// UI commands and review actions (all submitted through validated controller actions).
prepend('src/ui/tui/studio-view.ts','import type{SkillWizard}from"./skill-wizard.js";\n');
change('src/ui/tui/studio-view.ts','export interface StudioIntent {','export interface StudioIntent {\n wizard?:SkillWizard;');
change('src/ui/tui/studio-view.ts','export const studioScreens = [','export const studioScreens = [\n "skill-trials", "trial-detail",');
change('src/ui/tui/studio-view.ts','  if (screen === "skills") {','  if(screen === "skill-trials"){for(const trial of p.trials)result.push(row("trial:"+trial.trialId,trial.profileId+" · "+trial.status,trial.pairs.map(p=>p.verdict).join(", "),JSON.stringify(trial,null,2)));if(!p.trials.length)result.push(row("help","Sin evaluaciones","Seleccioná una habilidad y proponé una comparación A/B",HELP));}\n  else if(screen === "trial-detail"){const trial=p.trials.find(t=>t.trialId===subject);if(!trial)return [];result.push(row("trial-report","Ver informe y evidencia",trial.status,JSON.stringify(trial,null,2)));if(trial.status==="proposed"||trial.status==="interrupted")result.push(row("trial-authorize","Autorizar la evaluación","Solo versiones inspeccionadas; no aprueba producción",JSON.stringify(trial,null,2)));if(trial.status==="authorized")result.push(row("trial-run","Ejecutar evaluación real",`${trial.budget.maxRequests} requests · ${trial.budget.maxTokens} tokens como límite`,JSON.stringify(trial,null,2)));if(trial.status==="running")result.push(row("trial-cancel","Cancelar evaluación","Conserva evidencia y presupuesto ya utilizado"));}\n  else if (screen === "skills") {');
change('src/ui/tui/studio-view.ts','    result.push(row("help", "Cómo importar y asignar", "Formato, permisos y desactivación", HELP));','    result.push(row("help", "Cómo importar y asignar", "Formato, permisos y desactivación", HELP));');
// Locate robust method boundaries rather than guessing formatting of individual rows.
change('src/ui/tui/studio-view.ts','  return result;','  if(screen==="skills"){result.push(row("collect-draft","Recopilar borrador terminado","Desde una goal del creador verificada por Judge"));result.push(row("trials","Evaluaciones A/B","Respuestas y código con ejecución independiente"));}\n  if(screen==="skill-detail"){const skill=p.skills.find(v=>v.releaseId===subject);if(skill){const selected=p.control.manual.some(x=>x.releaseId===skill.releaseId&&x.hash===skill.hash);result.splice(2,0,row("manual-select",selected?"Quitar selección manual":"Seleccionar manualmente",selected?"Esta versión está seleccionada":"Solo para sus equipos autorizados",JSON.stringify(skill.pins,null,2),selected));result.push(row("evaluate-response","Proponer A/B de respuestas","Autoriza un borrador sin activarlo en producción"));result.push(row("evaluate-code","Proponer A/B de código","Comprobaciones reales en sandbox, sin acceso al grader"));}}\n  return result;');
change('src/ui/tui/studio-view.ts','  const c = p.config,','  if(screen==="skills"&&id==="creator")return {wizard:"create"};\n  if(screen==="skills"&&id==="collect-draft")return {wizard:"collect"};\n  if(screen==="skills"&&id==="trials")return {screen:"skill-trials"};\n  if(screen==="skill-trials"&&id.startsWith("trial:"))return {screen:"trial-detail",subject:id.slice(6)};\n  if(screen==="trial-detail"){const trial=p.trials.find(t=>t.trialId===subject);if(!trial)return;\n    if(id==="trial-report")return {action:action({command:"trial-report",trialId:trial.trialId})};\n    if(id==="trial-cancel")return {action:action({command:"trial-cancel",trialId:trial.trialId})};\n    if(id==="trial-run")return confirmed({command:"trial-run",trialId:trial.trialId,confirmation:"EJECUTAR"},"Ejecutar A/B",JSON.stringify(trial,null,2)+"\\nConsume cuota de la cuenta elegida. No cambia modelo ni aprueba automáticamente.","EJECUTAR");\n    if(id==="trial-authorize"){const versions=[trial.releaseId,trial.baselineReleaseId].filter(Boolean);const inspected=versions.map(rid=>p.skills.find(s=>s.releaseId===rid)?.inspectionId);if(inspected.some(i=>!i))return {notice:"Inspeccioná primero cada versión desde /habilidades."};return confirmed({command:"trial-authorize",trialId:trial.trialId,specHash:trial.specHash,inspectionIds:inspected as string[],confirmation:"EVALUAR"},"Autorizar contrato A/B",JSON.stringify(trial,null,2)+"\\nNo activa la habilidad en producción.","EVALUAR");}\n  }\n  if(screen==="skill-detail"){const skill=p.skills.find(v=>v.releaseId===subject);if(!skill)return;if(id==="manual-select"){if(!skill.reviewed||!skill.enabled)return {notice:"Revisá y habilitá esta versión antes de seleccionarla. Para evaluar el borrador usá A/B."};const selected=!p.control.manual.some(x=>x.releaseId===skill.releaseId&&x.hash===skill.hash);return confirmed({command:"manual-select",releaseId:skill.releaseId,hash:skill.hash,selected,expectedEpoch:p.control.epoch,pins:selected?skill.pins:[],confirmation:"SELECCIONAR"},"Cambiar selección manual",JSON.stringify(selected?skill.pins:[],null,2)+"\\nLas exclusiones de equipo prevalecen. Se reinician sesiones afectadas.","SELECCIONAR");}if(id==="evaluate-response"||id==="evaluate-code")return {wizard:id};}\n  const c = p.config,');
// Remove a stale no-op anchor introduced by older formatting variations only when present.
change('src/ui/tui/studio-view.ts','  if (names[name])','  if(name==="evaluaciones")return {screen:"skill-trials",action:action({command:"status"})};\n  if(name==="crear-skill")return {wizard:"create"};\n  if (names[name])');
prepend('src/ui/tui/app.tsx','import{GuidedForm,type FormSpec}from"./form.js";\nimport{skillWizard}from"./skill-wizard.js";\n');
change('src/ui/tui/app.tsx','  const [bindingProfile, setBindingProfile] = useState<string>();','  const [bindingProfile, setBindingProfile] = useState<string>();\n  const [wizard,setWizard]=useState<FormSpec>();');
change('src/ui/tui/app.tsx','  const applyStudioIntent = (intent: StudioIntent) => {','  const applyStudioIntent = (intent: StudioIntent) => {\n    if(intent.wizard){try{setWizard(skillWizard(intent.wizard,s,studioSubject,(action,title,body,phrase)=>{setWizard(undefined);setConfirmation({action,title,body,phrase});}));}catch(error){notify(String(error));}return;}');
change('src/ui/tui/app.tsx','    if (confirmation || bindingProfile) return;','    if (confirmation || bindingProfile || wizard) return;');
change('src/ui/tui/app.tsx','      {confirmation && (','      {wizard&&<GuidedForm spec={wizard} width={width} height={height} motion={motion} onCancel={()=>setWizard(undefined)}/>}\n      {confirmation && (');
change('src/i18n/es.ts','const screenNames: Readonly<Record<string, string>> = {','const screenNames: Readonly<Record<string, string>> = {\n "skill-trials":"Evaluaciones A/B", "trial-detail":"Comparación y evidencia",');
change('src/ui/tui/views.ts','export const commands: PaletteItem[] = [','export const commands: PaletteItem[] = [\n {name:"evaluaciones",description:"Contratos y resultados A/B de habilidades"},');
console.log('Prepared actual SDK E2E, guided creator/forms and independently verified Blender pipeline.');
