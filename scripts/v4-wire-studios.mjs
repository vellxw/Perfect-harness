import fs from 'node:fs';
const change=(path,before,after)=>{const text=fs.readFileSync(path,'utf8');if(text.split(before).length!==2)throw Error('Anchor not unique: '+path+' '+before.slice(0,110));fs.writeFileSync(path,text.replace(before,after));};
const prepend=(path,text)=>fs.writeFileSync(path,text+fs.readFileSync(path,'utf8'));
// Only the owned source tree is changed; no personal credentials or OS configuration.
change('src/integrations/unity/schema.ts','  descriptorFingerprint:','  descriptorPath: z.string().min(1).max(4096),\n  descriptorFingerprint:');
change('src/integrations/unity/registration.ts','descriptorFingerprint: seen.descriptorFingerprint, sourceCommit:','descriptorPath: seen.descriptorPath, descriptorFingerprint: seen.descriptorFingerprint, sourceCommit:');
fs.appendFileSync('src/integrations/unity/registration.ts',`\nexport async function verifyUnityBinding(policy: UnityPolicy): Promise<void> {\n const actual=await inspectUnityDescriptor(policy.descriptorPath);\n if(actual.descriptorFingerprint!==policy.descriptorFingerprint || actual.projectPath!==policy.projectPath || actual.editorPid!==policy.editorPid || actual.endpoint!==policy.endpoint) throw new Blocked("UNITY_BINDING_CHANGED","El Editor, proyecto o token cambió. Volvé a autorizar después de reconciliar operaciones inciertas.");\n try{process.kill(policy.editorPid,0);}catch{throw new Blocked("UNITY_EDITOR_STOPPED","Editor no disponible");}\n}\n`);
prepend('src/integrations/types.ts','import { UnityPolicySchema } from "./unity/schema.js";\n');
change('src/integrations/types.ts','      tools: z.record(toolName, EffectSchema),','      tools: z.record(toolName, EffectSchema),\n      unity: UnityPolicySchema.optional(),');
prepend('src/integrations/agent-session.ts',`import { unityEffect, validateUnityCall, validateUnityWorkspace } from "./unity/policy.js";
import { verifyUnityBinding } from "./unity/registration.js";
import { UnityLease } from "./unity/lease.js";
`);
change('src/integrations/agent-session.ts','  private available(): IntegrationRecord[] {','  private unityLeases = new Map<string, UnityLease>();\n  private available(): IntegrationRecord[] {');
change('src/integrations/agent-session.ts','          r.config.roles.includes(this.scope.role) &&','          r.config.roles.includes(this.scope.role) &&\n          (r.config.kind !== "mcp" || !r.config.unity || r.config.unity.profileIds.includes(this.request.run.profileId ?? this.scope.role)) &&');
change('src/integrations/agent-session.ts','    const fixed = this.records.get(server);','    if(record.config.kind === "mcp" && record.config.unity && !record.config.unity.profileIds.includes(this.request.run.profileId ?? this.scope.role)) throw new Blocked("UNITY_PROFILE_DENIED", "El perfil no tiene acceso a este Editor");\n    const fixed = this.records.get(server);');
change('src/integrations/agent-session.ts','        : Object.hasOwn(config.tools, t.name),','        : config.unity ? Boolean(unityEffect(config.unity, t.name)) : Object.hasOwn(config.tools, t.name),');
change('src/integrations/agent-session.ts','          : c.tools[tool];','          : c.unity ? unityEffect(c.unity, tool) : c.tools[tool];');
change('src/integrations/agent-session.ts','    let connection = this.wires.get(c.id);','    if(c.kind === "mcp" && c.unity) {\n      if(c.transport.type !== "http" || c.transport.url !== c.unity.endpoint || c.transport.bearerEnv !== "PERFECT_UNITY_TOKEN") throw new Blocked("UNITY_ENDPOINT_MISMATCH", "El transporte no coincide con el Editor autorizado");\n      await verifyUnityBinding(c.unity);\n    }\n    let connection = this.wires.get(c.id);');
change('src/integrations/agent-session.ts','      if (c.kind === "github") validateGithub(c, tool, args);','      if (c.kind === "github") validateGithub(c, tool, args);\n      if(c.kind === "mcp" && c.unity) {\n        validateUnityCall(c.unity,tool,args);\n        await validateUnityWorkspace(c.unity,{profileId:this.request.run.profileId ?? this.scope.role,cwd:this.request.cwd,readOnly:this.scope.readOnly,task:this.request.context.task},effect);\n      }');
change('src/integrations/agent-session.ts','      this.registry.claim(op.id, op.digest);','      if(c.kind === "mcp" && c.unity && effect !== "read") {\n        let lease=this.unityLeases.get(server);\n        if(!lease){lease=new UnityLease(this.home,c.unity,this.scope.runId);this.unityLeases.set(server,lease);}\n        await lease.acquire();\n        await verifyUnityBinding(c.unity);\n        await validateUnityWorkspace(c.unity,{profileId:this.request.run.profileId ?? this.scope.role,cwd:this.request.cwd,readOnly:this.scope.readOnly,task:this.request.context.task},effect);\n      }\n      this.registry.claim(op.id, op.digest);');
change('src/integrations/agent-session.ts','      } catch (error) {\n        this.registry.uncertain(','      } catch (error) {\n        if(effect !== "read") this.unityLeases.get(server)?.markUncertain();\n        this.registry.uncertain(');
change('src/integrations/agent-session.ts','    await this.queue.catch(() => {});\n    this.registry.interruptRun','    await this.queue.catch(() => {});\n    await Promise.allSettled([...this.unityLeases.values()].map(lease=>lease.close()));\n    this.registry.interruptRun');
change('src/integrations/agent-session.ts','    if (config.kind === "browser" || config.kind === "desktop") return record;','    if (config.kind === "browser" || config.kind === "desktop") return record;\n    if(config.kind === "mcp" && config.unity) await verifyUnityBinding(config.unity);');
// Add optional query/pagination to discovery while preserving existing default shape.
change('src/ports/integrations.ts','  list(server?: string): Promise<unknown>;','  list(server?: string, query?: string, offset?: number): Promise<unknown>;');
change('src/integrations/agent-session.ts','  async list(server?: string): Promise<unknown> {','  async list(server?: string, query = "", offset = 0): Promise<unknown> {');
change('src/integrations/agent-session.ts','      tools: this.tools(r)\n        .filter(','      totalTools: this.tools(r).length,\n      nextOffset: offset + 12 < this.tools(r).filter(t=>t.name.toLowerCase().includes(query.toLowerCase())).length ? offset + 12 : null,\n      tools: this.tools(r)\n        .filter(t=>t.name.toLowerCase().includes(query.toLowerCase()))\n        .slice(offset, offset + 12)\n        .filter(');
change('src/integrations/agent-session.ts','          description: t.description,','          description: t.description?.slice(0, 800),');
change('src/integrations/pi-tools.ts','Type.Object({ server: Type.Optional(Type.String()) }),','Type.Object({ server: Type.Optional(Type.String()), query: Type.Optional(Type.String()), offset: Type.Optional(Type.Integer({minimum:0,maximum:1000})) }),');
change('src/integrations/pi-tools.ts','      async (args) =>\n        integrations.list(\n          z.object({ server: z.string().optional() }).strict().parse(args)\n            .server,\n        ),','      async (args) => { const a=z.object({server:z.string().optional(),query:z.string().max(100).optional(),offset:z.number().int().min(0).max(1000).optional()}).strict().parse(args); return integrations.list(a.server,a.query,a.offset); },');
// Every imported byte must match the pinned repository tree, including license sources.
prepend('src/skills/remote.ts','import { createHash } from "node:crypto";\n');
change('src/skills/remote.ts','  const files: Record<string, Buffer> = Object.create(null) as Record<','  const files: Record<string, Buffer> = Object.create(null) as Record<');
change('src/skills/remote.ts','    const bytes = await fetchBytes(rawFile(entry.path), signal);','    const bytes = await fetchBytes(rawFile(entry.path), signal);\n    const actual=createHash("sha1").update(Buffer.from(`blob ${bytes.length}\\0`)).update(bytes).digest("hex");\n    if(actual !== entry.sha) throw new Blocked("SKILL_REMOTE_INTEGRITY","El contenido no coincide con el árbol fijado");');
change('src/skills/remote.ts','    const bytes = await fetchBytes(rawFile(path), signal, 300000);','    const bytes = await fetchBytes(rawFile(path), signal, 300000);\n    const entry=tree.tree.find(e=>e.path===path);\n    if(!entry || entry.type!=="blob" || !["100644","100755"].includes(entry.mode) || createHash("sha1").update(Buffer.from(`blob ${bytes.length}\\0`)).update(bytes).digest("hex")!==entry.sha) throw new Blocked("SKILL_LICENSE_INTEGRITY","La licencia no coincide con la versión fijada");');
// A loaded script is not a new permission; it still uses the command sandbox already granted to the writer.
change('src/skills/session.ts','  summary() {','  authorizedRelease(skillId: string): SkillRelease {\n    this.guard();\n    const release=this.loaded.get(skillId);\n    if(!release || !this.allowed(release)) throw new Blocked("SKILL_NOT_LOADED","Cargá primero la habilidad autorizada");\n    return release;\n  }\n  summary() {');
change('src/ports/agent-runtime.ts','  skills?: {','  skills?: {\n    run?(id: string, resource: string, args: string[]): Promise<unknown>;');
prepend('src/application/agent-executor.ts','import { executeSkillScript } from "../skills/scripts.js";\nimport { assertNativeProfile } from "../modes/capabilities.js";\n');
change('src/application/agent-executor.ts','    await this.runtime.resolve(\n      this.definition(goal, role, task),','    assertNativeProfile(this.profile(goal, role, task).profile.id);\n    await this.runtime.resolve(\n      this.definition(goal, role, task),');
change('src/application/agent-executor.ts','          read: (skillId, resource) => skillSession.read(skillId, resource),','          read: (skillId, resource) => skillSession.read(skillId, resource),\n          ...(!definition.readOnly && services.command ? {run:(skillId:string,resource:string,args:string[])=>executeSkillScript({goal:input.goal,workspace:input.workspace,release:skillSession.authorizedRelease(skillId),resource,args,config:this.config,runner:this.runner,signal,guard:()=>skillSession.guard()})} : {}),');
prepend('src/adapters/pi/tools.ts','import { SkillScriptSchema } from "../../skills/scripts.js";\n');
change('src/adapters/pi/tools.ts','  return tools;','  if(request.services.skills?.run) tools.push(define("skill_run", "Ejecutar un script de una habilidad cargada dentro del sandbox. No se importa código generado ni se amplían permisos.", Type.Object({id:Type.String(),resource:Type.String(),args:Type.Optional(Type.Array(Type.String()))}), async raw=>{const a=SkillScriptSchema.parse(raw);return request.services.skills!.run!(a.id,a.resource,a.args);}));\n  return tools;');
// User diagnostics, without installing editors or claiming that a catalog is a smoke test.
change('src/modes/capabilities.ts','postgres:18.0-bookworm','postgres:18.6-bookworm');
change('src/skills/actions.ts','export const StudioActionSchema = z.discriminatedUnion("command", [','export const StudioActionSchema = z.discriminatedUnion("command", [\n  z.object({command:z.literal("capabilities")}).strict(),');
prepend('src/skills/admin.ts','import { studioCapabilities } from "../modes/capabilities.js";\n');
change('src/skills/admin.ts','    switch (action.command) {','    switch (action.command) {\n      case "capabilities": return {message:"Capacidades locales de los estudios",content:JSON.stringify(await studioCapabilities(),null,2)};');
prepend('src/cli/main.ts','import { registerUnityCommands } from "./unity.js";\n');
change('src/cli/main.ts','  registerSkills(program, context);','  registerSkills(program, context);\n  registerUnityCommands(program, context);');
change('src/cli/skills.ts','  root.command("lock")','  root.command("capacidades").action(()=>run(async()=>({command:"capabilities"})));\n  root.command("lock")');
change('src/ui/tui/studio-view.ts','  if (names[name])','  if(name === "capacidades") return {action:action({command:"capabilities"})};\n  if(name === "unity") return {notice:"Conexión segura: perfect unity inspeccionar <descriptor>; luego conectar con fingerprint y perfiles. El token se ingresa localmente en /integraciones. Consultá docs/unity.md.",screen:"integrations"};\n  if (names[name])');
change('src/ui/tui/views.ts','export const commands: PaletteItem[] = [','export const commands: PaletteItem[] = [\n  {name:"unity",description:"Conectar el Editor al MCP local con proyecto autorizado"},\n  {name:"capacidades",description:"Comprobar Blender, PostgreSQL y requisitos de editores"},');
console.log('Studios adapters wired: scoped Unity, bounded discovery, verified imports and sandboxed skill scripts.');
